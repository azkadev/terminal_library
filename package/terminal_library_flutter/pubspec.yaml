name: terminal_library_flutter
description: "A new Flutter package project."
version: 0.0.1

publish_to: none
environment:
  sdk: '>=3.4.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  terminal_library_flutter_pty:
    path: ../terminal_library_flutter_pty

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

 
flutter:  